// Single source of truth for the closed category list, matching the
// suggestion_category enum in database-schema.sql and the Category
// dropdown options in the PRD (Section 2.2 / Section 3).
export const VALID_CATEGORIES = ["Feature", "UI", "UX", "Enhancement", "Bug"];
